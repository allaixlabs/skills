# Codex 구현 지시서 — {{제목}}

> 이 문서는 단독 실행 스펙이다. 이 문서 밖의 대화 컨텍스트는 존재하지 않는다.
> 여기 적힌 범위만, 적힌 대로 구현하라. 재계획·범위 확장·무관 리팩토링 금지.
> 스펙이 모호하거나 전제가 틀렸으면 임의 판단하지 말고 최종 보고에 질문으로 남겨라.

## Mission (1줄)

{{한 문장 목표 — 예: "/ 랜딩페이지 히어로·타이포·간격을 아래 스펙대로 개선"}}

## Context

- 프로젝트 루트: {{절대경로}}
- 스택: {{프레임워크 / 언어 / 스타일 시스템(예: Next 15 + Tailwind v4)}}
- dev 서버: `{{명령}}` → {{URL}} (이미 실행 중인지 여부)
- 빌드: `{{명령}}` / 테스트: `{{명령}}` / 린트: `{{명령}}`

## 변경 지시 (파일별)

### 1. {{상대경로/파일}}

- 현재: {{현재 상태 — Claude가 분석에서 확인한 사실}}
- 변경: {{구체 지시 — 수치/토큰/시그니처 수준. 추상어 금지}}

### 2. {{상대경로/파일}}

- 현재: {{...}}
- 변경: {{...}}

<!-- UI 작업이면 아래 섹션 유지, 아니면 삭제 -->
## 디자인 스펙 (UI 작업 시)

- 타이포: {{폰트/스케일/자간/행간 수치}}
- 컬러: {{토큰/HEX — 기존 토큰 우선, 신규 토큰은 정의 위치 명시}}
- 간격/레이아웃: {{spacing 스케일, 컨테이너 폭, 브레이크포인트별 동작}}
- 모션: {{transition/duration — 과한 애니메이션 금지 등}}
- 첨부 이미지: before 스크린샷 {{N}}장 — 현재 상태 참고용이며 목표 시안이 아님

## Out of scope (금지)

- {{건드리면 안 되는 파일/디렉터리/동작}}
- 의존성 추가 금지(위에 명시된 것 제외)
- 스펙과 무관한 리팩토링·포맷팅-only 변경 금지
- {{스키마/보안/시크릿/결제/배포 등 인간 승인 영역}}

## Acceptance Criteria

- [ ] {{기준 1 — 확인 방법 포함: 예 "`curl -s {{URL}}` 응답에 X 포함"}}
- [ ] 빌드 통과: `{{명령}}` exit 0
- [ ] 기존 테스트 통과: `{{명령}}`
- [ ] `git diff` 범위가 위 '변경 지시' 파일 목록을 벗어나지 않음

## 완료 보고 형식 (최종 메시지에 반드시 포함)

1. 변경 파일 목록 — 경로별 1줄 요약
2. Acceptance Criteria 각각의 충족 여부 + 실행 증거(명령과 출력 요약)
3. 스펙에서 벗어난 결정이 있었다면 그 사유, 모호했던 지점이 있었다면 질문
